import ConfigScreen, {
  AppInstallationParameters as _AppInstallationParameters,
} from './ConfigScreen';

export default ConfigScreen;
export type AppInstallationParameters = _AppInstallationParameters;
